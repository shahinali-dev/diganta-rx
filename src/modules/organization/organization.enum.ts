// CompanySize is defined once, in prisma/schema.prisma, and Prisma generates
// this enum on `npx prisma generate`. Re-exported here so other modules can
// keep importing it from the module folder (same convention as user.enum.ts).
export { CompanySize } from "@prisma/client";
